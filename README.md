# moved

The Grok Build plugin is **[ThewindMom/grok-pstack](https://github.com/ThewindMom/grok-pstack)**.

This repository was an incomplete upload. Do not `grok plugin install ThewindMom/pstack`.

```bash
grok plugin install ThewindMom/grok-pstack --trust
grok plugin enable grok-pstack
```

Then `/setup-pstack` and `/poteto-mode`.
